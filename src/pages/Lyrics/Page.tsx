import { useMemo, useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import { useSongs } from '../../hooks/useCatalog';
import { getLyricsMap } from '../../services/lyrics';
export function Lyrics(){const {data:songs,loading}=useSongs();const [search,setSearch]=useState('');const [ids,setIds]=useState<Set<string>>(new Set());useEffect(()=>{if(songs.length)getLyricsMap(songs.map(s=>s.id)).then(m=>setIds(new Set(m.keys()))).catch(()=>null)},[songs]);const groups=useMemo(()=>{const result=new Map<string,typeof songs>();songs.filter(s=>ids.has(s.id)&&s.title.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>a.title.localeCompare(b.title)).forEach(s=>{const k=s.title.trim().charAt(0).toUpperCase()||'#';const arr=result.get(k)??[];arr.push(s);result.set(k,arr)});return [...result.entries()].sort(([a],[b])=>a.localeCompare(b))},[songs,ids,search]);return <div className="page"><header className="page-hero"><span className="eyebrow">A–Z</span><h1>Lyrics</h1><p>The words behind the records.</p></header><section className="content-section"><input className="search-input" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search songs…"/>{loading?<div className="loading-state">Building lyric index…</div>:groups.length?<div className="lyrics-index">{groups.map(([letter,items])=><section key={letter}><h2>{letter}</h2>{items.map(s=><Link key={s.id} to={`/lyrics/${s.slug}`}>{s.title}<span>{s.artist_name}</span></Link>)}</section>)}</div>:<div className="empty-state">No lyrics have been added yet.</div>}</section></div>}
