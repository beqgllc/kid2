import { FormEvent, useState } from 'react';
import { sendFanMail } from '../../services/fanMail';
export function FanMail(){const [name,setName]=useState('');const [email,setEmail]=useState('');const [message,setMessage]=useState('');const [state,setState]=useState<'idle'|'sending'|'success'|'error'>('idle');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setState('sending');setError('');try{await sendFanMail(name,email,message);setState('success');setName('');setEmail('');setMessage('')}catch(err){setState('error');setError(err instanceof Error?err.message:'Unable to send.')}};return <div className="page"><header className="page-hero"><span className="eyebrow">CONTACT</span><h1>Fan mail</h1><p>Say whatever you need to say.</p></header><section className="form-card"><form onSubmit={submit}><label>Name<input value={name} onChange={e=>setName(e.target.value)} maxLength={100}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} maxLength={254}/></label><label>Message<textarea required value={message} onChange={e=>setMessage(e.target.value)} maxLength={5000}/></label><button className="button" disabled={state==='sending'}>{state==='sending'?'Sending…':'Send message'}</button>{state==='success'&&<p className="form-success">Message received. Thanks for reaching out.</p>}{state==='error'&&<p className="form-error">{error}</p>}</form></section></div>}
